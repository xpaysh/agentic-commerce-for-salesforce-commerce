version: "3.8"
services:
  agentic-commerce:
    build: ..
    ports:
      - "8787:8787"
    env_file:
      - ../.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:8787/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
